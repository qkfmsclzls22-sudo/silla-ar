# Silla AR 작업 기준

- 기준 저장소는 `https://github.com/qkfmsclzls22-sudo/silla-ar`다.
- 사용자는 2026-09-06에 수정된 3D 파일을 이 저장소에 올리고, 이후 수정할 때마다 같은 저장소에 반영하라고 요청했다. 사용자는 이곳에서 결과를 검토하고 추가 보완을 요청한다.
- 요청된 수정이 끝나면 관련 모델·페이지 경로·검토 자료를 함께 반영하고, 실제 커밋과 푸시 성공 여부를 확인해 전달한다. 업로드가 막히면 로컬 준비 완료와 원격 반영 완료를 구분한다.
- 모델을 교체할 때 각 페이지의 `model-viewer` 경로를 확인한다. GLB를 갱신하면서 이전 모델의 USDZ를 계속 연결하지 않는다. 같은 수정본으로 만든 USDZ를 연결하거나, 지원되는 경우 `model-viewer`의 자동 USDZ 생성을 사용한다.
- 새 모델 파일명에 버전을 넣어 이전 파일의 캐시와 구분한다. 해당 작업과 관계없는 유물·페이지는 수정하지 않는다.
- 실제 모델의 전후 비교와 남은 한계를 `seondeok/review/` 같은 관련 자산의 검토 폴더에 기록한다. 생성 이미지를 실제 3D 렌더 검증으로 제시하지 않는다.
- 사용자의 최신 명시적 요청을 우선하며, 파일 검증·브라우저 검증·실기기 AR 검증을 서로 구분한다.
- 사용자는 2026-09-07에 복식 고증은 필수가 아니며 선덕여왕 느낌을 살리면 된다고 밝혔다. 특히 얼굴의 어색함을 최우선으로 개선하라고 요청했다. 향후 같은 모델을 보완할 때 이 방향을 우선한다.
